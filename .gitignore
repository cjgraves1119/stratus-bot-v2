node_modules/
.env
*.log
.DS_Store

# Wrangler local secret files — never commit
.dev.vars
*.dev.vars

# Claude Code local scratch (agent worktrees, session files) — never commit
.claude/

# Secrets file — lives in parent Bots/ directory, but add here as
# belt-and-suspenders in case someone ever copies it into the repo tree.
.stratus-secrets
*.stratus-secrets
