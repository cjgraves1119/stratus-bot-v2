{
  "name": "stratus-ai-worker",
  "version": "1.0.0",
  "description": "Stratus AI — Claude-powered Cisco/Meraki Webex quote bot (Cloudflare Workers)",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
