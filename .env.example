# Webex bot token (from developer.webex.com — your new Stratus AI bot)
WEBEX_BOT_TOKEN=your_webex_bot_token_here

# Anthropic API key (for Claude fallback)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Port (default 3000 — leave as-is for local)
PORT=3000
